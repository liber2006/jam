name: Build

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      artifact-prefix:
        required: false
        type: string
        default: ''
      version:
        required: false
        type: string
        default: ''
      sign:
        required: false
        type: boolean
        default: false

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            platform: mac
          - os: windows-latest
            platform: win
          - os: ubuntu-latest
            platform: linux
    runs-on: ${{ matrix.os }}
    environment: ${{ inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Enable Corepack
        run: corepack enable

      - name: Stamp version
        if: inputs.version != ''
        run: |
          node -e "
            const fs = require('fs');
            const pkg = JSON.parse(fs.readFileSync('apps/desktop/package.json', 'utf8'));
            pkg.version = '${{ inputs.version }}';
            fs.writeFileSync('apps/desktop/package.json', JSON.stringify(pkg, null, 2) + '\n');
          "

      - name: Install dependencies
        run: yarn install

      - name: Install Linux dependencies
        if: matrix.platform == 'linux'
        run: sudo apt-get update && sudo apt-get install -y libsecret-1-dev

      # macOS: Write the App Store Connect API key to disk for notarization
      - name: Prepare macOS signing
        if: matrix.platform == 'mac' && inputs.sign
        env:
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
        run: |
          mkdir -p ~/private_keys
          echo "$APPLE_API_KEY" | base64 --decode > ~/private_keys/AuthKey_${{ secrets.APPLE_API_KEY_ID }}.p8

      # Signed build (release) — code signing only, notarization submitted separately
      - name: Build (signed)
        if: inputs.sign
        timeout-minutes: 15
        run: yarn workspace @jam/desktop electron:build --publish never
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}

      # Submit for notarization without waiting — Apple processes in background,
      # Gatekeeper checks online when users download (stapling not required)
      - name: Notarize macOS artifacts (no-wait)
        if: matrix.platform == 'mac' && inputs.sign
        run: |
          for f in apps/desktop/release/*.dmg apps/desktop/release/*.zip; do
            if [ -f "$f" ]; then
              echo "Submitting $(basename "$f") for notarization..."
              xcrun notarytool submit "$f" \
                --key ~/private_keys/AuthKey_${{ secrets.APPLE_API_KEY_ID }}.p8 \
                --key-id ${{ secrets.APPLE_API_KEY_ID }} \
                --issuer ${{ secrets.APPLE_API_ISSUER }} \
                --no-wait
            fi
          done

      # Unsigned build (staging) — fast, no notarization wait
      - name: Build (unsigned)
        if: ${{ !inputs.sign }}
        run: yarn workspace @jam/desktop electron:build --publish never
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: 'false'

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: ${{ inputs.artifact-prefix }}${{ matrix.platform }}
          path: |
            apps/desktop/release/*.dmg
            apps/desktop/release/*.zip
            apps/desktop/release/*.exe
            apps/desktop/release/*.AppImage
            apps/desktop/release/*.deb
          if-no-files-found: warn
